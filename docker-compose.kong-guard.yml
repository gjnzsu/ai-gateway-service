services:
  ai-gateway-service:
    build:
      context: .
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-poc-openai-key}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-poc-deepseek-key}
    ports:
      - "4000:4000"

  kong-guard:
    image: kong:3
    depends_on:
      - ai-gateway-service
    environment:
      KONG_DATABASE: "off"
      KONG_DECLARATIVE_CONFIG: /kong/declarative/kong-prompt-guard-evaluation.yml
      KONG_PROXY_ACCESS_LOG: /dev/stdout
      KONG_ADMIN_ACCESS_LOG: /dev/stdout
      KONG_PROXY_ERROR_LOG: /dev/stderr
      KONG_ADMIN_ERROR_LOG: /dev/stderr
      KONG_ADMIN_LISTEN: "0.0.0.0:8201"
    ports:
      - "8200:8000"
      - "8201:8201"
    volumes:
      - ./kong/kong-prompt-guard-evaluation.yml:/kong/declarative/kong-prompt-guard-evaluation.yml:ro
